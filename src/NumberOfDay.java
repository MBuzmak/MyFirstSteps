public class NumberOfDay {
    public static boolean isWeekend(int weekDayNumber) {
        if (weekDayNumber == 6) {
            return (true);
        } else if (weekDayNumber == 7) {
            return (true);
        }
        return (false);
    }
}
